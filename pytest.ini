[pytest]
testpaths = tests
# Warnungen sichtbar machen: eine CI, in der Warnungen untergehen, verliert
# genau die Vorstufe zum Fehler.
filterwarnings =
    error::DeprecationWarning
    default
addopts = --strict-markers
